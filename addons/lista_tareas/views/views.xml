<odoo>
  <data>
    <!-- DEFINICIÓN DE VISTAS -->

    <!-- Listado de tareas principal -->
    <record model="ir.ui.view" id="lista_tareas.list">
      <field name="name">lista_tareas list</field>
      <field name="model">lista_tareas.lista_tareas</field>
      <field name="arch" type="xml">
        <list>
          <field name="tipo"/>
          <field name="fecha_limite"/>
          <field name="pendiente"/>
          <field name="realizada"/>
          <field name="fallida"/>
        </list>
      </field>
    </record>

    <!-- Listado de tareas formato Kanban -->
    <record id="lista_tareas.view_kanban" model="ir.ui.view">
      <field name="name">lista_tareas kanban</field>
      <field name="model">lista_tareas.lista_tareas</field>
      <field name="arch" type="xml">
        <kanban class="o_kanban_mobile" sample="1" quick_create="false">
          <progressbar field="activity_state"
            colors='{"planned": "success", "today": "warning", "overdue": "danger"}'/>
          <templates>
            <t t-name="card">
              <div class="d-flex mb-2" style="justify-content: space-between;">
                <field name="tipo" class="fw-bolder fs-5" />
                <field name="fecha_limite" class="ms-1 text-muted fs-5"/>
              </div>
              <footer>
                <div class="d-flex text-muted">
                  <field name="description"/>
                </div>
              </footer>
            </t>
          </templates>
        </kanban>
      </field>
    </record>

    <!-- Listado de tareas formato Calendar -->
    <record id="lista_tareas.view_calendar" model="ir.ui.view">
      <field name="name">lista_tareas calendar</field>
      <field name="model">lista_tareas.lista_tareas</field>
      <field name="arch" type="xml">
        <calendar string="Tareas" date_start="fecha_limite" color="tipo">
          <field name="tipo"/>
          <field name="description"/>
          <field name="pendiente"/>
          <field name="realizada"/>
        </calendar>
      </field>
    </record>


    <!-- ACCIONES DE VENTANA -->

    <!-- Accionador vista Lista Tareas -->
    <record id="lista_tareas.action_window" model="ir.actions.act_window">
      <field name="name">Listado principal</field>
      <field name="res_model">lista_tareas.lista_tareas</field>
      <field name="view_mode">list,form</field>
    </record>

    <!-- Accionador vista Kanban -->
    <record id="lista_tareas.action_kanban" model="ir.actions.act_window">
      <field name="name">Tareas formato Kanban</field>
      <field name="res_model">lista_tareas.lista_tareas</field>
      <field name="view_mode">kanban,form,list</field>
      <field name="view_id" ref="lista_tareas.view_kanban"/>
    </record>

    <!-- Accionador vista Calendar -->
    <record id="lista_tareas.action_calendar" model="ir.actions.act_window">
      <field name="name">Tareas formato Calendar</field>
      <field name="res_model">lista_tareas.lista_tareas</field>
      <field name="view_mode">calendar,form,list</field>
      <field name="view_id" ref="lista_tareas.view_calendar"/>
    </record>
	

    <!-- ELEMENTOS MENÚ CON ACCIONADORES -->

    <!-- Principal -->
    <menuitem name="Lista-Tareas" id="lista_tareas.menu_root" 
      action="lista_tareas.action_window"/>

    <!-- Adyacentes -->
    <menuitem name="Formato Kanban" id="lista_tareas.menu_kanban" 
      parent="lista_tareas.menu_root" action="lista_tareas.action_kanban"/>

    <menuitem name="Formato Calendario" id="lista_tareas.menu_calendario" 
      parent="lista_tareas.menu_root" action="lista_tareas.action_calendar"/>

  </data>
</odoo>
